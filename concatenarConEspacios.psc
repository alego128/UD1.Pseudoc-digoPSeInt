Algoritmo concatenarConEspacios
	
	definir a, b como cadena 
	escribir "escribe dos cadenas "
	leer a
	leer b
	escribir concatenaConEspacios(a,b)
	
FinAlgoritmo

Funcion salida <- concatenaConEspacios(a,b)
	salida = concatenar(a,b)
FinFuncion
	